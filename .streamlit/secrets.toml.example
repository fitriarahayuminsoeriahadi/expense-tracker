# Salin file ini jadi "secrets.toml" (tanpa .example) di folder yang sama,
# lalu isi DATABASE_URL dengan connection string dari Neon.
# JANGAN upload/share file secrets.toml ke publik (GitHub dsb) - ini berisi
# kredensial database.

DATABASE_URL = "postgresql://user:password@host/dbname?sslmode=require"
